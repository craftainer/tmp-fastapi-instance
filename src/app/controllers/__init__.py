"""FastAPI routers with no resource of their own (audit/mock).

The generic CRUD router factories live in `crud.controllers` instead;
the health check router lives in the sibling `health` package.
"""
